#include <stdio.h>

typedef unsigned long long u64;

u64 setBit(int bit) {
  return 0;
  // sets the square, marking a position on the  board, input type yet
  // undecided!
}

int main() {
  const u64 t = 0xffffffffffffffff;

  printf("%llu", t);
  return 0;
}
